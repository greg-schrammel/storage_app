import * as React from 'react';
import { SafeAreaView, Text } from 'react-native';

function ConfigScreen(): React.ReactElement {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>Config</Text>
    </SafeAreaView>
  );
}

export default ConfigScreen;
